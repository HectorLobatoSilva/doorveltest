import React from "react";

const Parent = () => {
    return (
        <>
            <small>
                <i>Selecciona un tipo de propiedad de la barra lateral</i>
            </small>
        </>
    );
};

export default Parent;
