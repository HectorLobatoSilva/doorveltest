import React from "react";
import { useDataContext } from "Context";

const Home = () => {
    return (
        <div>
            <h1>Home Page</h1>
            <small>
                <i>
                    Use la barra de navegación para ver nuestra lista de
                    amenidades
                </i>
            </small>
        </div>
    );
};
export default Home;
